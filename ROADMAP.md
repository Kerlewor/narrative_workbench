# Narrative Workbench v0.3.0 及后续改进路线图

> 基于项目根目录下的 `v0.3.0及后续改进思路` 重构。
> 核心判断：**复杂度不能消失，但可以从主模型上下文里搬出去。**
> 旧版路线图已废弃，本文档为唯一版本。

---

## 零、核心诊断

当前项目最大的问题不是结构太多，而是**结构的承载方式仍主要依赖大段提示词和 Markdown 注入**。必要的基础设施（章节状态、伏笔追踪、角色一致性、Context Packet、四阶段 Agent、共创模式、知识索引、确定性脚本门禁）已经存在，但它们还没有充分转化为"由程序维护、按需检索、分角色编译"的底层系统。

后续不是"删提示词"，而是两件事：

1. **把必要结构从"模型需要反复阅读的说明书"，改造成"系统已经执行好的约束与检索结果"。**
2. **把作者体验从"记命令、读文件、判断该调用谁"，改造成"打开工作台就知道今天写什么、哪里冲突、修改了什么"。**

---

## 一、当前关键问题清单（修复优先级：极高）

| # | 问题 | 表现 | 后果 |
|---|---|---|---|
| 1 | **CLAUDE.md 过重** | 同时承担启动协议、状态规则、Agent 流程、共创说明、写前写后 SOP，接近完整系统手册 | 每次启动让主模型先"读工厂说明书"，而非聚焦当前章节 |
| 2 | **Context Packet 未做真正相关性筛选** | context_builder 遍历全部角色卡；摘要与伏笔截取基于文件前段而非章节位置 | 角色越多上下文越膨胀，重要信息反被挤掉 |
| 3 | **上下文重复注入** | prompt_compiler 拼入 Agent 说明 + 项目规则 + 上下文包，上下文包自身又包含风格/状态/规则 | 同一约束多次出现，token 浪费且可能版本冲突 |
| 4 | **知识索引不是检索系统** | knowledge_index 基于关键词与元数据扫描，章节参数未控制检索相关性 | 无法回答"这一场戏真正涉及哪些事实" |
| 5 | **Agent 持久化可能反向累积** | 持久会话节省初始化成本，但若干章后历史累积反而越来越重 | 前几章省了 token，后几章翻倍还回去 |
| 6 | **Claude/Codex 平台适配不对称** | 有 `.claude/agents` 但无 Codex 原生 `AGENTS.md` | "支持 Codex CLI" 的体验依赖用户手工提示，非平台原生接入 |
| 7 | **共创脚本存在静默截断** | review_author_chapter.py 和 polish_author_chapter.py 对作者正文使用了前 5000 字符截取 | 长章节后半段未进入审阅/润色上下文，作者误以为全章已处理 |

### 三个必须立即修复的实现层风险

**风险 1："相关角色"实际是"全部角色"**

`context_builder.py` 构建角色上下文时遍历角色目录中所有文件并逐一截取，而非根据当前章节计划的出场人物筛选。十几个角色时即成为 token 黑洞。

**风险 2："上一章摘要"不一定是上一章**

脚本读取总摘要文件开头内容，而非严格定位当前章节的前一章摘要；伏笔也截取总文件前部。小说越长，模型拿到的信息越"早期很多、当前很少"。

**风险 3：prompt_compiler.py 疑似参数缺陷**

构建任务层时的回退逻辑引用了未显式传入的 `agent` 变量。此外若脚本将项目根路径固定为仓库根目录，在 `examples/` 下运行时会读取模板状态而非示例项目状态。

---

## 二、提示词工程重构：从"大段注入"到"任务包编译"

### 2.1 重新定义主模型职责

| 现状（主模型承担） | 目标（主模型只负责） |
|---|---|
| 读完整工作流规则 | — |
| 判断该读哪些文件 | — |
| 理解状态协议 | — |
| 组织上下文 | — |
| 调用 Agent | 调用 Agent |
| 维护状态 | — |
| 判断冲突 | 判断冲突 |
| 创作协调 | 创作协调 |

> **主模型只负责当前创作决策与高层协调；状态筛选、上下文拼接、重复消除、事实追溯、预算限制，由程序层完成。**

### 2.2 CLAUDE.md 从系统手册缩为启动入口

现状：`CLAUDE.md` 混合了不变规则、搭建说明、Agent 调用方式、章节生命周期、共创工作流、状态更新协议、伏笔协议、写作前后 SOP。

目标拆分：

```text
CLAUDE.md                  # 极短启动入口，仅保留不可违反原则与路由方式（60-100 行）
AGENTS.md                  # Codex 对应入口，与 CLAUDE.md 保持同源
workflow/
  constitution.md          # 完整系统原则，供维护者与验证器使用
  lifecycle.md             # 章节生命周期说明
  state_contract.md        # 状态协议
  hook_protocol.md         # 伏笔协议
skills/
  plan_chapter/
  write_chapter/
  review_chapter/
  polish_author_draft/
  import_outline/
  deepen_character/
```

新 `CLAUDE.md` 不再告诉模型"所有事情怎样做"，只告诉它：
- canonical 文件永远优先于工作草稿
- 当前任务必须先调用 packet compiler
- 需要写章、审查、润色、导入大纲时分别调用哪个 skill
- 不得绕过门禁直接写入正式章节

| 内容 | 当前状态 | 目标 |
|---|---|---|
| 启动入口 | 完整系统说明书 | 60-100 行核心路由 |
| 流程规则 | 混在主入口中 | 拆入 Skills 与验证器 |
| Claude/Codex 适配 | Claude 明确，Codex 弱 | 同源生成 CLAUDE.md 与 AGENTS.md |
| 重复规则注入 | 每轮可能重复 | 稳定规则使用版本哈希，仅变化时重新发送 |

### 2.3 结构化小说事实账本（ledger + views 双轨制）

Markdown 适合作者阅读，不适合程序精准检索。保持 Markdown-native 可读性的同时，底层增加结构化账本：

```text
story/
  ledger/
    facts.jsonl              # 程序检索与编译依据
    hooks.jsonl
    timeline.jsonl
    characters.jsonl
    relationships.jsonl
    knowledge_boundaries.jsonl
    locations.jsonl
  views/
    pending_hooks.md         # 作者可阅读、可 Git 比对的渲染视图
    current_state.md
    character_cards/
    timeline.md
```

每条结构化事实保存其文本证据来源。伏笔示例：

```json
{
  "id": "HOOK_017",
  "name": "林安认出旧雪牌",
  "status": "open",
  "introduced_in": "chapter_03",
  "last_touched": "chapter_11",
  "due_window": [18, 24],
  "related_characters": ["lin_an", "zhou_yue"],
  "reader_knows": true,
  "lin_an_knows": false,
  "evidence_refs": ["chapter_03:p42", "chapter_11:p18"]
}
```

规划第 19 章时，系统精确检索：本章涉及角色；该角色目前知道什么；哪些伏笔已到推进窗口；哪些秘密禁止泄露；哪些事实必须与旧章节一致。而不是把整个 `pending_hooks.md` 和全部角色卡塞给模型。

### 2.4 Context Builder → Relevance Resolver（相关性解析器）

Context Packet 从"拼文件摘要"升级为"解析当前任务依赖"。

**章节计划的 structured frontmatter：**

```yaml
chapter: 19
pov: lin_an
cast_ids: [lin_an, zhou_yue]
location_ids: [old_station]
hook_ids: [HOOK_017, HOOK_023]
secret_ids: [SECRET_004]
knowledge_domains: [雪牌来历, 林安与周月关系进展]
required_previous_chapters: [17, 18]
```

**改进前后对比：**

| 当前做法 | 改进后 |
|---|---|
| 读取全部角色卡 | 只读取 `cast_ids` 对应角色及直接提及的重要缺席角色 |
| 截取摘要文件开头 | 精确读取上一章及计划指定的历史章节摘要 |
| 截取伏笔文件前 80 行 | 只读取 `hook_ids`、到期伏笔与高风险冲突伏笔 |
| 读取整体风格规则 | Writer 只读取场景所需风格差量；Polish 才读取较完整约束 |
| 预算超出后报警 | 在编译阶段主动裁剪、降级、说明省略原因 |

**每条注入信息必须写明原因：**

```yaml
included_context:
  - item: character/lin_an
    reason: 本章 POV 角色
  - item: hook/HOOK_017
    reason: 当前章节计划要求推进
  - item: chapter_18_summary
    reason: 上一章直接连续场景
omitted_context:
  - item: character/chen_yu
    reason: 本章无出场、无关系状态变化
```

### 2.5 不同 Agent 接收不同任务包

| Agent | 注入内容 | 不注入 | 非正文上下文预算目标 |
|---|---|---|---|
| **Writer** | 本章目标、场景节拍、出场人物状态、必须推进/禁止暴露的伏笔、上章连接信息、本章风格偏好 | 完整伏笔库、完整角色档案、全局规则 | 5000-8000 tokens |
| **Polish** | 当前草稿、风格差异提示、语言禁区、不得改变的事实列表、重点修复句段 | 完整伏笔库、全量角色卡 | 2000-4000 tokens |
| **Review** | 候选成稿、任务目标、相关事实证据、伏笔处理要求、连贯性与泄密边界、门禁脚本输出 | 不相关角色卡、不相关卷的摘要 | 6000-10000 tokens |
| **Fixer** | 成稿、Review 指出的问题、允许修改的段落范围、不得突破的事实边界 | 整个项目体系 | 2000-4000 tokens |
| **主协调模型启动入口** | 路由、不可违反规则 | 所有流程细节 | 1000-2000 tokens |

### 2.6 稳定规则的版本哈希 + 增量发送

不选择两个极端（每章全量发送 vs 永远依赖臃肿持久会话）：

```text
初始启动：
  发送 baseline capsule + 当前任务包

后续章节：
  若 baseline_hash 不变：
      仅发送章节 delta packet
  若角色规则/世界观/风格发生变化：
      发送 changed_items + 新 hash
  若超过上下文阈值或发生漂移：
      新开 Agent，会话重建自结构化账本，而非依赖旧聊天记录
```

底线：**Agent 记忆可以提高效率，但永远不能成为事实唯一来源。正式章节与结构化账本才是 canonical truth。**

### 2.7 先不做向量数据库

当前最缺的不是 embedding，而是准确的实体关系与引用约束。优先级：

1. 角色 ID、地点 ID、伏笔 ID、秘密 ID 标准化
2. 每章计划显式声明相关实体
3. 每条状态变化附带证据段落
4. 编译器根据显式依赖选择内容
5. 最后再为"模糊记忆检索""风格案例检索""大量设定问答"增加向量搜索

原因：小说连续性中最严重的错误不是"忘记一个类似句式"，而是某角色提前知道了不该知道的秘密、伏笔被遗忘或重复揭示、关系状态突然倒退、场景物理事实冲突。这些更适合结构化账本与证据链接。

---

## 三、作者便利功能：从"AI 流水线"到"创作工作台"（全 Markdown-native，无独立界面）

> 实现方案详见第九节。所有"看板"和"控制台"是脚本自动生成的 Markdown 文件。作者通过自然语言命令触发操作，Claude Code/Codex 负责交互。

### P0：写作控制台 → `story/DASHBOARD.md`

作者打开项目直接看到（自动生成的 Markdown 文件）：

```markdown
# 《临安雪》创作控制台

## 当前进度
- 当前章节：第 19 章 / 当前状态：待写作
- 当前 POV：林安

## 本章必须处理
- 推进伏笔：雪牌来源
- 保持周月回避态度

## 本章禁止出现
- 不得直接揭示周月身份
- 林安不得知道父亲曾到过旧站台

## 可直接输入的操作
- 为第 19 章生成写作简报
- 规划第 19 章场景卡
- 审查第 18 章
- 显示当前全部高风险伏笔
```

### P0：真正可用的作者草稿审阅与润色

**修复静默截断：** 取消 5000 字符硬截断，改为全文分块处理。

**全文分段审查：** 按场景分段 → 无场景标记则按自然段窗口分块 → 每块保留段落 ID → 审阅结论指向具体段落。

**修改建议为结构化 diff 报告（`story/runtime/chapter-XXXX.polish-diff.md`）：**

```markdown
## 修改建议 01
### 位置：第 12 段，第 2 句
### 原文
她看着那块雪牌，心里忽然有一种说不出来的感觉。
### 建议文本
她的手指停在雪牌缺口上，没有继续往下摸。
### 修改原因
原文直接说明情绪，建议改为动作承载迟疑感。
### 影响范围
仅语言表达，不改变事实、伏笔或人物行为。
```

作者回复"接受修改 01 和 03，拒绝修改 02"，平台负责执行。这比自建 UI 更易调试、更容易 Git 追踪。

**三种权限边界：**

| 模式 | AI 可以做 | 不可以做 |
|---|---|---|
| 语言润色 | 修改表达、节奏、重复与 AI 味 | 不得改事实、关系、剧情走向 |
| 场景增强 | 增加动作细节、环境反应、对话张力 | 不得新增关键事实或伏笔 |
| 剧情建议 | 提出结构优化与替代路线 | 不得自动写入正式稿 |

### P0：伏笔与连续性看板

**伏笔看板：**

| 伏笔 | 状态 | 首次出现 | 最近触碰 | 建议推进窗口 | 风险 |
|---|---|---|---|---|---|
| 雪牌来源 | 待推进 | 第3章 | 第11章 | 第18-24章 | 即将过久未回应 |
| 周月身份 | 隐藏中 | 第6章 | 第15章 | 第25章后 | 当前不可明示 |

**角色知识边界矩阵：**

| 事实 | 林安知道 | 周月知道 | 读者知道 |
|---|---|---|---|
| 雪牌来自旧站台 | 否 | 是 | 部分知道 |
| 林安父亲曾到站台 | 否 | 是 | 否 |

**时间线冲突检查：** 人物位置、事件日期、物品转移、年龄一致性。

**情绪与关系弧线图：** 本章前关系状态 → 本章变化 → 变化证据 → 是否跳跃过大。

### P1：场景卡与章节编排板

```text
场景 1：旧站台入口
POV：林安
目的：引出雪牌与旧站台的联系
冲突：周月试图阻止林安继续追查
结果：林安拿到旧车票
推进伏笔：HOOK_017
禁止泄露：SECRET_004
```

作者可用自然语言操作：添加场景、修改顺序（"将场景 2 移到场景 1 之前"）、修改 POV、绑定角色与伏笔、标记必须出现的物象或对白、一键编译为 Writer 任务包。

### P1：角色声音实验室

- 查看角色说话习惯
- 对比同一句话由不同角色说出的效果
- 检查当前章节对白是否出现"角色声音串线"
- 标记角色不会说的话、不会做的事
- 生成对话压力测试场景
- 作者确认后才写入角色卡

### P1：版本管理与分支试写

```text
chapter_019/
  author_v1.md
  polish_candidate_a.md
  polish_candidate_b.md
  review_report.md
  final.md
  decision_log.md
```

提供：段落级 diff、接受/拒绝单条修改、建立平行场景分支、合并前自动检查伏笔与事实变化、正式提交后自动更新账本。

### P2：拆解学习模式

输入样章，输出：叙事视角、场景切换规律、句式长短分布、情绪递进方式、对话与动作比例、信息隐藏方式、意象复现方法、章尾驱动力类型。

约束：不复制具体作者表述；输出为可迁移方法而非仿写。

### P2：导出 EPUB/DOCX/发布排版

方便最终交付的格式化导出。

---

## 四、建议版本路线

> **状态更新 (2026-05-31):** v0.2.2 和 v0.3.0 已全部完成交付。v0.4.0 为下一阶段。

### v0.2.2：正确性修复（不增加功能）✅ 已完成

1. 修复 `prompt_compiler.py` 中疑似未传入 `agent` 的回退路径问题
2. 所有脚本支持显式 `--project-root`，为 demo 项目建立端到端测试
3. Context Builder 根据章节计划筛选角色、伏笔和摘要，不再默认读取全部角色卡
4. 作者审阅与润色取消静默 5000 字符截断，改为全文分块处理
5. 每个 Context Packet 输出：token 估计、注入了哪些条目及原因、省略了哪些内容、是否超出预算

**此版完成后才能验证当前上下文系统是否真的有效。**

### v0.3：平台原生化 + Context Engine 重构 ✅ 已完成

**平台适配：**
1. 精简 `CLAUDE.md`（60-100 行核心路由）+ 新增 Codex 对应 `AGENTS.md`（同源生成）
2. Skills 同步脚本：`skills/`（唯一正式来源）→ 生成 `.claude/skills/` + `.agents/skills/` 轻量入口包装
3. 新增 `.codex/agents/` + `.codex/hooks.json`（Codex 自定义 Agent 与生命周期脚本绑定）
4. 将主要工作流（规划/写作/审查/润色/简报/导入大纲/深化角色）改造为平台原生 Skills，作者用自然语言触发

**上下文引擎：**
5. 引入结构化小说账本：facts、hooks、timeline、character knowledge、relationships、secrets（JSONL + Markdown views 双轨）
6. 新建 Relevance Resolver：根据章节计划检索相关事实；自动构建 Writer/Polish/Review/Fixer 四种不同任务包
7. Prompt Compiler 完成去重、预算限制与规则哈希

**全章统筹基础设施：**
8. 章节导演表模板与生成脚本（`story/plans/chapter_NNN_director_sheet.yaml`）
9. 场景接力卡自动生成（`story/runtime/chapter_NNN_scene_handoffs.yaml`）
10. 全章连续性审查模板（`story/runtime/chapter_NNN_coherence_review.md`）

**质量保障：**
11. 引入回归测试：某章必须包含哪些事实、不应包含哪些秘密、应检索哪些伏笔、上下文预算是否超限

**此版完成后：项目从"提示词框架"升级为"小说上下文引擎"，具备分场景写作+全章统筹的完整能力，且在 Claude Code 和 Codex 上均为原生可调用。**

### v0.4：核心引擎封装 + 统一运行入口

> 详细设计见 `/home/kerlewor/codex-safe/books/_frameworks/v0.3.0改进路线图`

**核心目标：** 将 v0.3.0 的 25 个独立脚本抽象为可复用的 `core/` 模块，新建统一 CLI 入口，封装模型调用接口（摆脱对 Claude Code/Codex 的硬依赖），最终通过 PyInstaller 打包为无需 Python 环境的可执行程序。

**架构变更：**
- 新增 `core/` — 10+ 个纯 Python 模块（project/chapter/context/ledger/hooks/style/knowledge/doctor/gatekeeper/providers）
- 新增 `cli/` — 统一 CLI 入口（`nw project create`, `nw chapter run`, `nw hook report` 等）
- 新增 `core/providers/` — 模型调用抽象层（Anthropic + OpenAI + DeepSeek）
- `scripts/` 保留为 thin wrapper（Agent 用户零影响）

**向后兼容：** Claude Code / Codex Agent 用户的所有命令和路径不变。`scripts/` 内部改为 `from core import ...` 委托调用。

### v0.5：作者体验增强（全 Markdown-native）

> 原 v0.4.0 计划中的作者功能顺延至 v0.5.0。v0.4.0 的 core/ 封装为这些功能提供更快的实现基础。

**写作控制台与看板：** `story/DASHBOARD.md` 自动生成、伏笔看板 + 角色知识边界矩阵 + 时间线冲突检查

**共创审查与润色：** 全文分段审阅（段落 ID 锚定）、结构化 diff 报告 + 按编号接受/拒绝、润色全章节奏保护

**场景编排与角色：** 场景卡 Markdown 模板 + 自然语言编辑、角色声音实验室

**全章统筹闭环：** 章节双循环工作流、不同长度章节差异化策略、自动同步结构化账本

**扩展：** 版本管理与分支试写、文笔拆解训练、EPUB/DOCX 导出

---

## 五、功能优先级总览

| 优先级 | 功能 | 作者价值 | 对底层复用程度 |
|---|---|---|---|
| P0 | Context Packet 精准筛选与去重 | 直接减少模型负担与错误 | 极高 |
| P0 | 脚本路径/编译器参数/全文截断修复 | 避免错误结果与误导作者 | 极高 |
| P0 | 章节导演表 + 场景接力卡（模板与生成脚本） | 分场景写作不丢失连贯性 | 极高 |
| P0 | 全章连续性审查（因果/情绪/信息/物理/节奏五链） | 长章不被写成碎片拼贴 | 高 |
| P0 | `story/DASHBOARD.md` 自动生成 | 作者每天打开就知道写什么 | 高 |
| P0 | 全文审阅 + 结构化 diff 报告 + 按编号接受/拒绝 | 共创模式真正可用 | 高 |
| P0 | 伏笔看板 + 角色知识边界矩阵（Markdown 表格） | 形成项目核心竞争力 | 高 |
| P0 | Skills 同步脚本（skills/ → .claude/skills/ + .agents/skills/） | 平台原生调用，降低上下文负担 | 极高 |
| P1 | CLAUDE.md 精简 + AGENTS.md 新增 | 启动入口不再成为负担 | 高 |
| P1 | 章节双循环工作流（内循环+外循环） | 写作→审查→修订形成闭环 | 中高 |
| P1 | 不同长度章节差异化策略自动匹配 | 系统适应作者而非作者适应系统 | 中 |
| P1 | 场景卡 Markdown 模板 + 自然语言编辑 | 提高章节规划效率 | 中高 |
| P1 | 角色声音实验室（同句 × 不同角色对比） | 提高人物辨识度 | 中 |
| P1 | 润色全章节奏保护（style_arc 约束） | 局部润色不抹平全章节奏 | 中 |
| P1 | 版本管理与分支试写 | 提高作者控制感 | 中 |
| P2 | 文笔拆解训练模块 | 扩展新用户场景 | 中 |
| P2 | 导出 EPUB/DOCX/发布排版 | 方便最终交付 | 中低 |

> 注意：以上不包含任何独立 TUI 或 Web 界面。Claude Code 和 Codex 已提供交互外壳。

---

## 六、不应做的事

1. **不要继续把新功能说明追加进 `CLAUDE.md`** — 新流程进 Skill，新规则进验证器，新事实进账本
2. **不要以为更长上下文窗口就能解决问题** — 需要的是"更准的上下文"，不是"更大的上下文"。长上下文带来成本增加、信息被噪音淹没、注意力分散、旧-新状态冲突
3. **不要过早增加更多 Agent** — Writer/Polish/Review/Fixer/Librarian 已足够。新增 Agent 须满足：输入范围更窄、输出明确可验证、明显减少主模型负担
4. **不要把 Agent 会话记忆当成事实库** — 会话记忆适合保持工作风格，不适合承担最终事实。作者修改章节后旧会话可能保留过期认知
5. **不要先做花哨网页再补底层检索** — 上下文筛选不准时，漂亮界面只是把错误包装得更好看。先完成账本、检索、任务包与 diff，再做 UI
6. **现阶段不要做 TUI 或独立界面** — Claude Code 和 Codex 已经是你的交互界面。TUI 不解决核心问题（上下文精准度、状态管理、共创审查），却会背上跨平台兼容、安装升级、界面维护等一整套软件维护负担。正确路线是：底层能力 → 平台原生 Skill/Agent/Hook → Markdown 控制台与 diff 报告 → 作者更方便。只在 Markdown 确实无法满足需求时才重新评估界面

---

## 七、成功衡量指标

### 上下文效率

| 指标 | 目标 |
|---|---|
| 启动入口 token 数 | ≤ 2000 |
| Writer 非正文任务包 token 数 | 常规章节 ≤ 8000 |
| 重复规则占任务包比例 | ≤ 5% |
| 无关角色卡被注入数量 | 0 |
| 当前章相关伏笔召回率 | 测试集 ≥ 95% |
| 静默正文截断 | 0 |
| 超预算时无说明继续运行 | 0 |

### 作者体验

| 指标 | 观察方式 |
|---|---|
| 作者找到下一步操作所需时间 | 是否能在控制台一屏完成 |
| 作者为维持伏笔与状态手工查文件次数 | 应持续下降 |
| AI 修改被作者接受的比例 | 判断润色是否真正有用 |
| 作者因 AI 改坏剧情而撤回的次数 | 判断权限边界是否有效 |
| 每章从草稿到正式稿的操作步骤 | 是否逐步减少 |

---

## 八、产品定位：平台原生小说工作流包，而非独立应用

### 8.1 为什么不做 TUI

**现阶段不建议做 TUI 或独立界面。** Claude Code 和 Codex 已经是你的交互界面——它们提供了自然语言入口、文件读取器、命令执行器、Agent 调度外壳、权限审批和基本任务交互。你需要做的是让它们更懂你的小说工程，而不是自己重新造一个终端界面。

TUI 在当前阶段会带来不必要的负担：

1. **不解决核心问题。** 真正需要解决的是角色卡/伏笔/规则注入太多、Context Packet 不够精准、主模型读取负担太重。即使做出好看的 TUI，如果后台仍把无关角色卡和规则塞给模型，模型还是会漏、会跑偏。
2. **额外背上软件维护负担。** 安装升级、跨平台兼容（Windows/WSL/Linux）、路径编码、键盘操作与页面状态、文件编辑器、diff 展示、与 Claude Code/Codex 的调用衔接——这些与"让 AI 更好地辅助写小说"关系不直接，却大量消耗精力。
3. **平台已有的能力不需要重复实现。** Claude Code 支持 CLAUDE.md、Skills、Subagents、Hooks、自然语言与斜杠命令触发工作流。Codex 对应支持 AGENTS.md、Skills、Subagents、Hooks、CLI/IDE Extension。这些已经是你的外壳。

只有在以下条件满足时才需要重新考虑独立界面：有较多用户根本不会安装 Claude Code 或 Codex；作者强烈需要可视化拖动场景卡；伏笔和人物关系复杂到 Markdown 表格无法阅读；项目准备做成面向大众的独立产品。

### 8.2 重新明确的定位

> **Narrative Workbench 是一个运行在 Claude Code 与 Codex 之上的、Markdown-native、作者可控的长篇小说工程工作流系统。**

它不是独立写作软件，也不是新的聊天界面，而是：
- 一套小说工程目录
- 一套可追踪的状态账本
- 一组按需触发的平台原生 Skills
- 若干专业 Agent
- 一组确定性 Python 脚本
- 一套让作者用自然语言调用的写作流程

作者不需要学编程，直接在 Claude Code 或 Codex 里说"创建项目《临安雪》"、"为第 19 章生成写作简报"、"只润色对白不改变剧情"、"显示未来五章需要推进的伏笔"。平台负责交互，项目负责把这些自然语言请求转化成可靠、受控、低上下文负担的流程。

### 8.3 三层架构模型

```text
第一层：小说项目核心数据层（平台无关，长期护城河）
  story/canon/        正式设定、角色、世界观
  chapters/           正式章节
  plans/              章节计划与场景卡
  ledger/             结构化账本（hooks.jsonl, facts.jsonl, timeline.jsonl, secrets.jsonl, relationships.jsonl）
  runtime/            临时任务包、审阅结果、候选稿
  views/              渲染为 Markdown 的作者可读视图

第二层：确定性脚本层（平台无关）
  scripts/            负责模型不该"凭感觉"处理的事：
                      相关性检索、写作简报生成、事实冲突检查、
                      伏笔看板输出、作者原稿 vs AI 建议稿 diff、
                      AI 是否误改剧情的校验、Agent 精简任务包生成

第三层：平台适配层（分别适配 Claude Code 与 Codex）
  CLAUDE.md           Claude Code 入口（60-100 行核心路由）
  AGENTS.md           Codex 入口（同源生成）
  .claude/agents/     Claude Code Subagent 注册
  .claude/skills/     Claude Code 原生 Skills（由同步脚本从 skills/ 生成）
  .agents/skills/     Codex 原生 Skills（由同步脚本从 skills/ 生成）
  .codex/agents/      Codex 自定义 Agent 注册
  .codex/hooks.json   Codex 生命周期脚本绑定
```

**Skills 同步策略：** `skills/` 保留为唯一正式来源，运行同步脚本生成 `.claude/skills/` 和 `.agents/skills/` 的轻量入口包装。两个适配文件只负责告诉平台：什么时候调用、需要运行哪个脚本、应该读取哪些项目文件、输出保存在哪里。不手工维护三套重复内容。

### 8.4 Claude 优先、Codex 兼容的路线

当前仓库已有 `.claude/agents/`，Agent 结构更贴近 Claude Code。建议：

**第一阶段（v0.3）：Claude Code 作为主要体验平台。** 打磨：创建项目、导入大纲、生成写作简报、手写章节审查、局部润色、伏笔看板、Context Packet 精简、作者原稿与 AI 修改的 diff。

**第二阶段（v0.3 后期）：为 Codex 增加可靠适配。** 新增 AGENTS.md、`.agents/skills/`、`.codex/agents/`、`.codex/hooks.json`，保证核心功能（生成简报、审查作者稿、润色与 diff、更新伏笔看板、执行门禁脚本、读取项目状态）在 Codex 下可运行。不必在第一时间完全复刻 Claude Code 的所有交互习惯。

---

## 九、作者便利功能的 Markdown-native 实现方案

不做 TUI 不意味着放弃作者体验。所有便利功能可以通过"自然语言命令 + 自动生成可读 Markdown 文件"实现。

### 9.1 写作控制台 → `story/DASHBOARD.md`

每次运行状态更新后自动生成：

```markdown
# 《临安雪》创作控制台

## 当前进度
- 当前章节：第 19 章
- 当前状态：待写作
- 上一章：已定稿
- 当前 POV：林安

## 本章必须处理
- 推进伏笔：雪牌来源
- 保持周月回避态度
- 结尾需要形成继续调查的动力

## 本章禁止出现
- 不得直接揭示周月身份
- 林安不得知道父亲曾到过旧站台

## 待处理问题
- 第 17 章伏笔已接近推进窗口
- 第 11 章中的旧车票细节需要保持一致

## 可直接输入的操作
- 为第 19 章生成写作简报
- 规划第 19 章场景卡
- 审查第 18 章
- 显示当前全部高风险伏笔
```

作者打开文件就看到项目状态，在 Claude Code/Codex 里直接输入指令继续创作。这已覆盖 TUI 最核心的价值，几乎不需要开发新界面。

### 9.2 伏笔看板 → `story/views/hook_dashboard.md`

自动生成的 Markdown 表格：

| 伏笔 | 首次出现 | 最近推进 | 当前状态 | 建议处理章节 | 风险 |
|---|---|---|---|---|---|
| 雪牌来源 | 第3章 | 第11章 | 待推进 | 第18-21章 | 高 |
| 周月身份 | 第6章 | 第15章 | 隐藏中 | 第25章后 | 不可提前揭示 |

作者输入"更新伏笔看板"或"告诉我未来三章最需要推进的伏笔"，系统运行脚本并输出结果。

### 9.3 润色 diff 报告 → `story/runtime/chapter-XXXX.polish-diff.md`

不做界面按钮，输出结构化 diff 报告：

```markdown
## 修改建议 01

### 位置
第 12 段，第 2 句

### 原文
她看着那块雪牌，心里忽然有一种说不出来的感觉。

### 建议文本
她的手指停在雪牌缺口上，没有继续往下摸。

### 修改原因
原文直接说明情绪，建议改为动作承载迟疑感。

### 影响范围
仅语言表达，不改变事实、伏笔或人物行为。
```

作者回复"接受修改 01 和 03，拒绝修改 02"，平台负责执行修改、生成新版本与更新日志。这比自建 TUI 更易调试、更容易 Git 追踪。

### 9.4 场景卡 → `story/plans/` 目录下的 Markdown 文件

场景卡仍是 Markdown 文件（见 2.4 节的 structured frontmatter），作者可用自然语言创建和修改。拖动排序等交互操作用命令替代（"将场景 2 移到场景 1 之前"）。

---

## 十、分段处理与全章统筹：解决长章节不连贯问题

### 10.1 分段处理的五类不连贯风险

分段写作和分段审查在控制 token 方面是正确方向，但如果不加全章统筹，会产生系统性问题：

| 风险类型 | 表现 | 根因 |
|---|---|---|
| **场景接口断裂** | 上一段结尾林安情绪变冷，下一段开头两人平静交谈 | 下一场景未继承上一场景的即时状态 |
| **情绪重复启动** | 每个场景开头重新描写紧张、重复建立怀疑、同一段关系升温写两三遍 | 模型不知道"这个情绪已经建立过了" |
| **信息释放节奏失控** | 第一场该暗示的内容在第二场就被说透，悬念提前耗尽 | 模型只看到当前场景的信息目标，不知道全章释放计划 |
| **物品/位置/动作状态混乱** | 照片已放进口袋又出现在桌上；下雨但衣服没湿；角色已离开又无说明出现 | 场景衔接层面的局部连续性错误 |
| **全章节奏不协调** | 每个场景都追求"有张力""有金句""有钩子"，整章像短视频爆点拼接 | 没有全章情绪曲线约束 |

核心判断：**分段写作与分段润色是合理的；分段后完全不做全章统筹，则是不合理的。** 另外，1 万字章节不应被机械地切碎——审查阶段尤其需要保留全章视野，真正应被压缩的是大量设定、规则和历史资料，而非完整正文。

### 10.2 核心原则：分场景创作，全章节统筹

```text
整章计划（导演表）
   ↓
分场景写作（每场携带接力状态）
   ↓
场景之间传递衔接状态
   ↓
合并完整章节
   ↓
全章连续性审查（因果链/情绪链/信息链/物理状态/节奏曲线）
   ↓
针对问题局部修订
   ↓
全章最终复核
   ↓
正式定稿与状态更新
```

不同任务对分段的容忍度完全不同：

| 任务 | 是否应分段 | 原因 |
|---|---|---|
| 章节规划 | 不分段理解，分场景组织 | 必须先看到整章目标和推进链 |
| AI 辅助写作 | 适合按场景写 | 作者可中途控制，生成质量更稳 |
| 语言润色 | 适合按场景或段落处理 | 便于生成局部修改建议 |
| **连续性审查** | **应尽量阅读全文** | 主要检查前后照应、信息释放与情绪连续 |
| 伏笔与事实抽取 | 读取最终全文 | 避免遗漏后段变化 |
| **最终节奏检查** | **必须全章视角** | 需要判断整章起伏和推进结构 |

### 10.3 三个核心中间文件

不需要 TUI，不需要复杂数据库，三个新文件即可显著降低断裂风险。

#### 文件 1：章节导演表 `story/plans/chapter_019_director_sheet.yaml`

比场景卡更高层的全章蓝图：

```yaml
chapter: 19
title: 雪牌背面的名字
pov: 林安

chapter_purpose:
  plot: 确认雪牌与旧站台的联系
  relationship: 林安与周月产生第一次明显信任裂缝
  mystery: 推进雪牌来历，但保留周月真实身份

opening_state:
  location: 民宿走廊
  lin_an_emotion: 克制怀疑
  zhou_yue_emotion: 不安回避

closing_state:
  location: 旧站台候车室
  lin_an_emotion: 决定独自调查
  zhou_yue_emotion: 想阻止但无法解释
  relationship_change: 信任下降一级

emotional_arc:
  start: 怀疑但仍愿意相信周月
  middle: 因周月回避而产生隔阂
  end: 决定绕过周月独自调查

information_release:
  scene_01: 暗示雪牌异常
  scene_02: 证明周月认得雪牌
  scene_03: 确认雪牌来自旧站台
  scene_04: 保留周月身份秘密

forbidden_reveals:
  - 周月真实身份
  - 林安父亲到过旧站台的事实

style_arc:           # 全章语言节奏曲线
  scene_01: 冷静、迟疑、留白较多
  scene_02: 对话压迫感增强，但不爆发
  scene_03: 信息确认后句式变短
  scene_04: 冲突结束，以安静和疏离收尾

scene_chain:
  - id: scene_01
    role: 引发
    input_state: 林安注意到雪牌异常
    output_state: 他怀疑周月认得雪牌

  - id: scene_02
    role: 对抗
    input_state: 林安试探周月
    output_state: 周月回避，信任裂缝形成

  - id: scene_03
    role: 证实
    input_state: 林安独自查证
    output_state: 找到旧车票，确认站台关联

  - id: scene_04
    role: 留钩
    input_state: 周月追来阻止
    output_state: 林安拒绝相信她，悬念保留
```

它的作用不是增加提示词，而是**减少每个场景临时猜测整章逻辑的成本**。Writer 不需要读取完整大纲，只需要读取：本章导演表 + 当前场景卡 + 上一场景接力状态 + 相关角色与伏笔事实。

#### 文件 2：场景接力卡 `story/runtime/chapter_019_scene_handoffs.yaml`

每写完一个场景后自动生成，是防止前后断裂最重要的文件：

```yaml
- scene_id: scene_02
  handoff_to: scene_03

  physical_state:
    location: 民宿厨房
    time: 傍晚，雨刚停
    items:
      lin_an_holding: 缺角雪牌
      zhou_yue_holding: 黑色雨伞

  emotional_state:
    lin_an: 已经察觉周月在隐瞒，语气转冷
    zhou_yue: 明显回避，但不愿激怒林安

  revealed_information:
    - 林安已发现周月认得雪牌
    - 林安尚不知道雪牌具体来历

  unresolved_tension:
    - 林安刚问出："你以前是不是来过这里？"
    - 周月没有回答

  required_next_scene_input:
    - 林安手中仍有雪牌
    - 周月的回避态度持续
    - 对话中断后的沉默氛围

  do_not_resolve_in_this_scene:
    - 不得在本场解释周月身份
    - 周月不得突然坦白
```

下一场景 Writer 必须从这个状态接着写，而不是重新启动人物关系。

#### 文件 3：全章连续性审查报告 `story/runtime/chapter_019_coherence_review.md`

所有场景合并后进行。Reviewer 应尽量读取完整正文，重点检查：

```markdown
# 第 19 章全章连续性审查

## 因果链
- Scene 01 → Scene 02：成立。
- Scene 02 → Scene 03：动机略弱，林安突然独自前往旧站台，
  建议补充触发理由。

## 情绪链
- 林安从怀疑到疏离：总体成立。
- Scene 03 开头情绪回落过快，需要承接上一场对话冲突。

## 信息释放
- 雪牌来源推进合理。
- 第 41 段疑似提前暴露周月与旧站台的长期联系 → 建议弱化。

## 物理连续性
- 第 18 段雪牌被周月拿走，但第 27 段默认仍在林安手中 → 冲突。

## 节奏曲线
- Scene 02-03 连续高压无喘息，建议在 Scene 03 开头加 2-3 句环境过渡。

## 关系变化
- 信任下降有铺垫，但 Scene 04 林安的拒绝略显突兀。
  建议在 Scene 03 中增加 1 处他试图再信周月一次但落空的细节。

## 建议修订位置
- 第 22–24 段：加强林安独自调查的动机
- 第 27 段：修复雪牌持有者矛盾
- 第 41 段：弱化身份提示
- Scene 03 开头：承接情绪 + 环境喘息
```

### 10.4 写作阶段：每个场景的四类必备输入

Writer 每次写一个场景时，不能只拿到自己的场景卡：

1. **全章导演表** — 本章整体推进图、情绪曲线、信息释放计划、禁止揭示内容、语言节奏曲线
2. **当前场景卡** — 场景目标、冲突、结果、必须推进的伏笔、禁止泄露的秘密
3. **上一场景接力状态** — 物理状态、情绪状态、已揭示信息、未解决冲突
4. **下一场景预留接口** — 本场结尾必须把球传到哪里（`required_end_state` / `do_not_resolve`）

### 10.5 审查双轨制：局部审查 + 全章审查

**局部审查（每场写完后，轻量）：**

检查单场是否完成目标、角色行为是否符合当前状态、是否擅自揭示秘密、是否出现明显事实错误。输出轻量报告，不必读取整章全部文字。

**全章审查（所有场景合并后，必须）：**

Reviewer 读取完整正文 + 导演表 + 相关角色状态 + 相关伏笔与秘密边界 + 上一章结尾状态。不需要全部章节正文、全部角色卡、完整世界观、所有未使用伏笔。重点检查因果链、情绪链、信息链、物理连续性、关系变化、节奏曲线、伏笔推进。只有将场景拼在一起才会暴露的问题，在此阶段集中发现。

> **全文审查不等于全项目注入。** 对于 1 万字章节，"完整正文 + 精简相关事实包"往往比"切成五段分别审、但每段都重新带很多背景资料"更稳，也可能更节省 token。

### 10.6 润色的全章节奏保护

语言润色按段落处理时风险甚至比写作阶段更隐蔽。如果 Polish Agent 每段都单独追求"更有张力"，可能把整章所有地方都改得意象浓烈、节奏紧促、情绪饱满——局部变漂亮了，全章节奏却被抹平。

润色也必须两层：

1. **局部修改** — 按场景提出具体句段修改建议
2. **全章节奏保护** — 润色前必须先读取导演表中的 `style_arc`，每个局部 Polish 必须服从整体语言曲线，而不是自行"把文字改得更好看"

### 10.7 章节双循环工作流

**内循环（场景完成循环）：**

```text
读取本章导演表
   ↓
生成/修改当前场景（携带接力状态 + 预留接口）
   ↓
轻量场景检查
   ↓
生成场景接力卡
   ↓
进入下一场景
```

**外循环（整章完成循环）：**

```text
合并全部场景
   ↓
全章因果、情绪、信息、物理连续性审查
   ↓
作者确认需要修改的问题
   ↓
局部修订
   ↓
全章节奏复核
   ↓
定稿并更新事实账本
```

### 10.8 不同长度章节的差异化策略

| 章节长度 | 写作方式 | 审查方式 |
|---|---|---|
| 3000 字以内 | 可整章生成或分场景 | 直接阅读全文审查 |
| 3000–6000 字 | 建议分场景生成 | 全文审查 + 局部润色 |
| 6000–10000 字 | 分场景生成，必须有接力卡 | 全文连续性审查 + 分场景语言审查 |
| 10000 字以上 | 强制导演表与场景接力卡 | 全文或分层全章审查，**禁止只做局部审查** |

"强制"不是限制作者，而是限制系统不能用粗糙工作流处理复杂长章。

### 10.9 切分优先级

当必须分段时，切分优先级为：

```text
场景边界 > 时间跳转 > 地点切换 > POV 切换 > 明显情绪阶段 > 字数窗口
```

只有在没有明确场景结构、单场本身过长（>5000 字）时，才按字数拆分，且必须保留前后若干自然段作为重叠上下文。禁止对一场完整的对峙戏在中间强行切开。

---

## 十一、鸿蒙 App 化路线图（v0.6.0 起双线并行）

> **定位声明：** Narrative Workbench 从 v0.6.0 起分为两条独立发展线——开源版本继续在本仓库迭代更新；闭源版本（API 服务层 + 鸿蒙客户端）在独立仓库中开发，不在此仓库交付。两条线共享 v0.5.0 之前的核心框架代码，此后各自演进。

### 11.1 从"AI 助手插件"到"移动端 App"的核心断层

当前项目 v0.3.0 是一个运行在 Claude Code/Codex CLI 之上的 Markdown + Python 协议框架。要做成鸿蒙 app，存在五个结构性断层：

#### 断层 1：运行环境不兼容

项目全部 25 个确定性脚本是 **Python 3.11+ 标准库**，项目协议与数据存储是本地文件系统的 Markdown + JSONL + YAML。鸿蒙原生开发语言是 **ArkTS + ArkUI**，不原生运行 Python。

两条路径：
- **后端路径（推荐）：** Python 核心抽到云端服务，鸿蒙 app 做纯客户端，通过网络 API 调用
- **重写路径：** 25 个脚本全部用 ArkTS 重写，本地运行——工作量大且后续维护成本高

#### 断层 2：没有 API/服务层

项目是单体文件系统框架——脚本直接读写本地 `story/`、`chapters/` 目录。没有 REST API、没有后端架构、没有数据库抽象、没有用户认证、没有多设备同步。这些需要从零构建。

#### 断层 3：AI 模型调用硬依赖 Claude Code/Codex

核心创作流水线（Writer → Polish → Review → Fixer → Gatekeeper）依赖 Claude Code 的 subagent 体系和对话外壳。v0.4.0 计划的 `core/providers/` 模型调用抽象层尚未构建。鸿蒙 app 中调用 LLM 需解决：API Key 管理、流式输出、上下文窗口管理（写一章需 8000+ token 上下文包）、网络稳定性、费用控制。

#### 断层 4：交互范式根本不同

项目 UX 全部是自然语言对话——"写第 3 章"、"显示伏笔看板"、"接受修改 01 和 03"。鸿蒙 app 需要全新 UI：章节管理、伏笔看板可视化、角色知识矩阵、润色 diff 的触屏接受/拒绝、场景卡拖拽编排。这些在路线图中目前都是 Markdown-native 方案（`DASHBOARD.md`、结构化 diff 报告），尚未涉及图形界面设计。

#### 断层 5：存储模型差异

项目假设一个本地 Git 仓库的文件系统（`chapters/`、`story/ledger/`、`story/views/`、`story/runtime/`）。移动端 app 需要至少：本地 SQLite 数据库、云端同步方案、离线写作支持、冲突合并策略。

### 11.2 版本推进路线（v0.6.0 起双线并行）

```text
共享开源阶段（本仓库）
════════════════════════════════
v0.3.0 ✅ 已完成
  平台原生化 + 上下文引擎重构
  25 个脚本、JSONL 账本、Relevance Resolver

v0.4.0 🚧 计划中 —— App 化的硬前提
  core/ 模块体系（10+ 模块）
  nw 统一 CLI 入口
  core/providers/ 模型调用抽象层（Anthropic/OpenAI/DeepSeek）
  摆脱对 Claude Code/Codex 的硬依赖
  PyInstaller 打包 + PyPI 发布
  scripts/ 改为 thin wrapper，委托 core/

v0.5.0 📋 规划中 —— App UI 的设计参考源
  story/DASHBOARD.md 写作控制台
  结构化 diff 报告 + 按编号接受/拒绝
  伏笔看板 + 角色知识边界矩阵
  场景卡 Markdown 模板 + 自然语言编辑
  章节双循环工作流
  角色声音实验室
  版本管理与分支试写
  EPUB/DOCX 导出
  ════════════════════════════════
  v0.5.0 为双线分叉点，以下分两条线独立演进

────────────────────────────────
开源线（本仓库，持续迭代）        闭源线（独立仓库）
────────────────────────────────
v0.6.0 开源 📋                  v0.6.0 闭源 🔒
  场景卡交互增强                    core/ → REST API 封装
  角色声音实验室完善                （FastAPI）
  协作模式优化                     本地→数据库抽象层
  多语言支持                       （SQLite + 云同步）
  社区 Skill 市场                  用户/项目多租户隔离
  ...持续迭代...                   LLM 流式 SSE 接口
                                   认证与 API Key 管理
v0.7.0 开源 📋                     章节版本云端存储
  写作分析面板
  批量章节管理                  v0.7.0 闭源 🔒
  第三方 AI 平台对接                ArkTS + ArkUI 原生开发
  ...持续迭代...                    调用 v0.6.0 API 服务
                                   触屏写作控制台
                                   伏笔/角色知识边界可视化
                                   润色 diff 触屏接受/拒绝
                                   场景卡拖拽编排
                                   离线写作 + 联网同步
                                   华为应用市场上架
```

### 11.3 为什么 v0.6.0 后分双线发展

1. **v0.5.0 是框架完整形态，之后两条线的用户不同。** 到 v0.5.0，Narrative Workbench 作为"AI 小说工程框架"的能力已经闭环。开源线继续服务 Claude Code/Codex 用户群体，持续打磨写作工作流、角色工具和社区生态。闭源线服务不想配置命令行和 AI 助手的终端消费者。

2. **v0.6.0 开始的两条线解决不同问题。** 开源线关注写作体验深度——场景编排、角色实验室、Skill 市场。闭源线关注服务化——API 封装、用户系统、数据库、云同步。它们从同一个 v0.5.0 代码基出发，但演进方向不再重合。

3. **闭源线涉及服务运营，不适合开源仓库。** API 服务层意味着运维服务器、管理 API Key 池、处理用户数据——这些是产品运营的范畴，不适合放在框架开源仓库中。

4. **鸿蒙 app 是商业产品。** 移动端应用涉及应用市场分发、用户付费、内容合规审核。AGPL-3.0 协议不适合移动端闭源分发，需独立开发并以商业许可发布。

5. **双线互相增益，而非相互竞争。** 开源版本培养作者社区、验证工作流创新、打磨核心算法。闭源 app 的开源内核保持与上游同步。两个版本的差异化定位避免了自我蚕食。

### 11.4 启动 App 工作的准入条件

| 条件 | 状态 | 说明 |
|---|---|---|
| v0.4.0 `core/` 模块化完成 | 必须 | 确保逻辑可复用、接口清晰，ArkTS 重写或 API 封装有据可依 |
| v0.4.0 模型抽象层完成 | 必须 | 确定 app 端 LLM 调用架构——直连 API 还是自建后端中转 |
| v0.5.0 Dashboard 验证完成 | 必须 | 验证作者真正需要看什么信息、做什么操作，再翻译成 ArkUI |
| v0.5.0 结构化 diff 验证完成 | 必须 | 验证触屏端"接受/拒绝修改"的交互逻辑 |
| v0.5.0 场景卡编排验证完成 | 建议 | 为移动端拖拽编排提供交互原型 |
| Python 后端 vs 纯客户端决策 | 必须 | 在 v0.5.0 完成后做最终架构决策 |

**底线建议：** 不要在 v0.5.0 完成之前启动鸿蒙 app 的实际开发。v0.4.0 确保逻辑可复用，v0.5.0 在 Markdown-native 层面验证全部交互模型。这两步走完后，鸿蒙 app 就是一个"对着已验证的后端 API 和交互逻辑写 ArkUI 客户端"的常规工程问题，而不是需要同时解决框架重构 + 平台迁移 + UI 设计 + 交互验证的巨型风险项目。

---

> 创建日期：2026-05-29
> 最后更新：2026-06-02（新增第十一节：鸿蒙 App 化路线图，v0.6.0 起双线并行）
> 本文档整合了三份分析材料（核心诊断与提示词工程重构 / 平台定位与 TUI 判断 / 分段处理与全章统筹），覆盖 v0.2.2 → v0.3 → v0.4 → v0.5 四阶段版本路线。
> v0.2.2 和 v0.3.0 已交付。下一阶段：v0.4.0 核心引擎封装。
> v0.6.0 起分为开源线（本仓库持续迭代）和闭源线（API 服务 + 鸿蒙 App，独立仓库）。
> 旧版路线图已废弃。
